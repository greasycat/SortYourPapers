[project]
name = "syp-prototype"
version = "0.1.0"
description = "Python prototype of the SortYourPapers ingest pipeline and folder watcher."
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.1.0",
  "openai>=1.60.0",
  "pypdf>=5.1.0",
  "python-dotenv>=1.0.1",
  "typer>=0.16.0",
  "watchdog>=6.0.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.4.0",
  "pytest-asyncio>=0.24.0",
]

[project.scripts]
sypy = "syp_prototype.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/syp_prototype"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
